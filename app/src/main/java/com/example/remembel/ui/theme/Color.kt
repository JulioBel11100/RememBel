package com.example.remembel.ui.theme

import androidx.compose.ui.graphics.Color

// Azul principal — el color de identidad de RememBel
val AzulPrincipal = Color(0xFF2563EB)
val AzulOscuro = Color(0xFF1E40AF)
val AzulClaro = Color(0xFFDBEAFE)

// Fondo cálido (un blanco roto, no un blanco frío de hospital)
val FondoCalido = Color(0xFFFFFBF5)
val SuperficieCalida = Color(0xFFFFF6EB)

// Texto
val TextoOscuro = Color(0xFF1C1B1F)
val TextoSuave = Color(0xFF5C5C5C)

// Estado de grabación (para cuando esté grabando de verdad, lo usaremos luego)
val RojoGrabando = Color(0xFFDC2626)